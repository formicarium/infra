# Default values for soil.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

deployment:
  replicaCount: 1

container:
  port: 8080

image:
  repository: formicarium/soil
  tag: eab7d6c34abfb3be0a44a00b0adb9c3ba63bc07a

service:
  port: 80

ingress:
  enabled: true
  tld: formicarium.host
