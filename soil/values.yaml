# Default values for soil.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

deployment:
  replicaCount: 1

container:
  port: 8080

image:
  repository: formicarium/soil
  tag: 0.0.7

service:
  port: 80

ingress:
  enabled: true
  tld: formicarium.host
